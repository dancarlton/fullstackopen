{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../part2/notes && npm run build && cp -r dist ../notes-backend",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && cd ../.. && git add . && git commit -m uibuild && git push",
    "logs:prod": "fly logs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dancarlton/fullstackopen.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/dancarlton/fullstackopen/issues"
  },
  "homepage": "https://github.com/dancarlton/fullstackopen#readme",
  "dependencies": {
    "express": "^4.21.2",
    "mongoose": "^8.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
